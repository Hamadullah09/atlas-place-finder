{
  "name": "place-finder",
  "version": "1.0.0",
  "private": true,
  "description": "Map-integrated place search: OpenStreetMap data, Wikimedia imagery, LLM cleaning, PDF + JPEG export",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck"
  },
  "license": "MIT"
}
